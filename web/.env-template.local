# API URL for development environment
DEV_API_URL=

# Session management secret key
# IMPORTANT: Generate a strong random secret (min 32 characters) with ```openssl rand -base64 32```
# This MUST match the SESSION_SECRET in your backend config
SESSION_SECRET=your-secret-key-must-be-at-least-32-characters-long-for-security